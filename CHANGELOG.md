# 更新紀錄

這份檔案記錄使用者看得到的功能新增與修正。技術細節與踩坑筆記在
`README.md` 的「已知的技術眉角」，這裡只寫「你會感覺到什麼不一樣」。

---

## 2026-09-08

### 檔案整理

- **出片產生的 json 不再跟成品混在一起**：`.timeline.json`、
  `.verify.words.json` 改放到 `06_meta/render_meta/`，`05_render/` 只會有
  mp4，要拿成品不用再從一堆 json 裡面找。搬家之前產生的舊成品仍然驗得動
  （驗證腳本會先找新位置，找不到才回頭找舊的）。

### 網頁編輯器（`caption_editor/`）

- **存檔鈕會顯示有沒有未存檔的改動**：有改動但還沒存檔時，按鈕會變成
  「存檔 ●」並轉為醒目色；存檔後恢復。
- **關閉或切換前會提醒**：有未存檔的改動時，關掉分頁、重新整理、切換專案
  都會先問一聲，不會再默默把改動弄丟。
  （提醒：GUI 的所有編輯都只存在瀏覽器裡，要按「存檔」才會寫回
  `edit_state.json`；按「產生影片」也會順便存檔。）

### 即時預覽

以前的即時預覽跟實際出片的成品長得不一樣，這次把它對齊到跟正片同一套
排版與設定。

- **9:16 預覽版面修正**：預覽現在跟成品一樣分成三段——上方標題區、中間影片、
  下方 CTA 區。之前 9:16 誤用了 16:9 那套「滿版影片＋文字疊在上面」的排版，
  所以字卡看起來全部疊在畫面上。
- **播放列改用「剪完後的時間軸」**：總長度顯示的是成品長度（例如 0:54），
  不再顯示原始素材的長度（例如 19:22）；進度條的範圍就是成品長度，被剪掉的
  片段拖不到，也拖不進去。
- **逐字編輯會自動反映到預覽**：在逐字編輯區刪字／斷句／設金句之後，預覽
  馬上更新，不用再多按一次「套用到字幕／剪輯點」。（存檔仍然要自己按
  「存檔」，這點沒變。）
- **剪掉目前停留的片段會自動跳走**：暫停狀態下把正在看的那段剪掉，預覽不會
  再卡在剛剛剪掉的畫面上，會自動跳到下一段保留片段的開頭。
- **字幕／字卡改用跟正片同一組設定**：描邊寬度、描邊顏色、陰影、金句放大與
  變色，全部改讀 `edit_state.json` 裡那組正片設定再依預覽比例換算，並且改成
  跟正片一樣「描邊在下、字色蓋在上」。修正之前描邊會把字吃掉、糊成一團黑的
  問題。
- **預覽框高度限制**：預覽框不會再高到把播放控制項擠出畫面外。

---

## 2026-09-05

- **字卡文字改用 ffmpeg drawtext 繪製**：標題卡／CTA 卡的文字不再經過
  ImageMagick，中文、冒號、百分比這類字元不會再把繪製指令弄壞。
  （注意：**ImageMagick 仍然是必要環境**，背景畫布還是用它產生，只是文字
  那一層換掉了。）
- **轉字幕遇到 API 限流會自動重試**：呼叫 Groq 轉字幕碰到 429（請求太頻繁）
  時會自動退避重試，長影片不會再整批失敗、要從頭來過。
- **修正長片段出現「幻影靜音」**：連續很長的保留片段會自動分段處理，出片
  不會再出現莫名其妙的無聲區段。
- **網頁 GUI 新增即時預覽與復原／重做**：編輯字幕時可以邊看大致效果，
  改錯了可以用「上一步／下一步」（Ctrl+Z / Ctrl+Y）回復。

---

## 2026-09-04

- **新增橫式 16:9 輸出**：除了直式 9:16 短影音，也可以輸出橫式版本，適合
  教學內容、螢幕錄影、要投影或放網頁的版本。用 `edit_state.json` 的
  `layout.ratio` 切換。
- **橫式標題改成左上角小標籤**：橫式版面的主影片改成滿版，標題縮成左上角
  的小角標，畫面不會再被大標題壓縮掉。
- **修正剪掉的片段會閃過幾格畫面**：被剪掉的內容現在真的不會出現在成品裡。
- **重複出片變快**：內容沒變的片段會沿用上次剪好的結果，只改字幕或字卡時
  重新出片快很多。
- **出片後的字幕同步驗證更穩**：`verify_render.py` 改成只上傳音軌去比對，
  驗證更快也更不容易失敗。
